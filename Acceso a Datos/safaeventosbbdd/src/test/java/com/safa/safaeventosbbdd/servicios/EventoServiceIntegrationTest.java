package com.safa.safaeventosbbdd.servicios;


import com.safa.safaeventosbbdd.dto.EventoDTO;
import com.safa.safaeventosbbdd.modelos.Evento;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.Mockito;

import java.util.Optional;

public class EventoServiceIntegrationTest {
/*
    @InjectMocks
    private EventoService service;

    @Mock
    private EventoService repository;


    @Test
    @DisplayName("Test de Integración → BuscarPorId()")
    public void buscarPorIdIntegrationTest(){
        //GIVEN
        Mockito.when(this.repository.getById(Mockito.anyInt())).thenReturn(Optional.of(new Evento()));

        //THEN
        var resultado = this.service.getById(1);

        //WHEN
        Mockito.verify(this.repository).getById(1);

        Assertions.assertNotNull(resultado, "El resultado no debe ser nulo");
        Assertions.assertEquals(1, resultado.getId(), "El ID del evento debe coincidir");

    }
*/
}
